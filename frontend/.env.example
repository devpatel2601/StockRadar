# No secrets needed in the frontend.
# The Vite proxy forwards /api calls to the Spring Boot backend.
# Backend URL is configured in vite.config.ts (proxy target).

# If deploying to production, set the API base URL here:
# VITE_API_BASE_URL=https://your-backend-domain.com
